/**
 * menlu - console utilities: table
 * ESM entry (thin wrapper around the CommonJS implementation)
 */

import consoleTable from "./table.js";

export const table = consoleTable.table;

export default { table };
