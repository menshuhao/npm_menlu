/**
 * menlu - console utilities: list
 * ESM entry (thin wrapper around the CommonJS implementation)
 */

import consoleList from "./list.js";

export const list = consoleList.list;

export default { list };
