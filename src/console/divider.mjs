/**
 * menlu - console utilities: divider
 * ESM entry (thin wrapper around the CommonJS implementation)
 */

import consoleDivider from "./divider.js";

export const divider = consoleDivider.divider;

export default { divider };
