/**
 * menlu - console utilities: box
 * ESM entry (thin wrapper around the CommonJS implementation)
 */

import consoleBox from "./box.js";

export const box = consoleBox.box;

export default { box };
